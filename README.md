# Frequency Visualizer

A simple Python app for visualizing frequencies from microphone input.

## Features
- Record frequencies over a set duration
- Display graphs of recorded frequencies
- Memory feature for multiple recordings
- Auto-adjust graph axes
- Save diagrams as PNG

## Installation
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python main.py`